import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function check() { const { data: mData, error: mErr } = await supabase.from('members').select('*').limit(1); console.log('Members table sample:', mData?.[0] ? Object.keys(mData[0]) : 'Empty or error', mErr); const { data: oData, error: oErr } = await supabase.from('email_otps').select('*').limit(1); console.log('OTPs table sample:', oData?.[0] ? Object.keys(oData[0]) : 'Empty or error', oErr); } check();
